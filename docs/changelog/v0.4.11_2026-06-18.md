# Changelog v0.4.11 - 2026-06-18

## li-xiu-qi

### 修复 (Bug Fixes)

- **macOS 桌面构建失败（sed 兼容性）**
  - 现象：v0.4.10 macOS 构建在 `Sync version from tag` 步骤失败
  - 根因：macOS BSD sed 的 `-i` 参数语法与 GNU sed 不同
  - 修复：改用 Python 替换 `pyproject.toml` 版本号，避免跨平台 sed 差异
  - 影响文件：`.github/workflows/ci-desktop.yml`

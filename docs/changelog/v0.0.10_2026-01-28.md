# Changelog v0.0.10 - 2026-01-28

##  li-xiu-qi

###  项目结构重构

#### 目录结构标准化
- 将项目从扁平结构重构为 `apps/` + `infra/` 分层结构
- 后端代码从 `Agentbackend/` 迁移到 `apps/backend/`
- 前端代码从 `frontend/` 迁移到 `apps/web/`
- 部署脚本移动到 `infra/deploy/`
- Docker 配置集中到 `infra/docker/`
- Supabase 配置移动到 `infra/supabase/`
- 归档文件移动到 `archive/reference_frameworks/`

#### Docker 配置更新
- 更新 `docker-compose.yml` 路径引用
- 更新 `docker-compose.prod.yml` 路径引用
- 修复 Celery Worker 模块路径 (`Agentbackend.app` → `app`)
- 修复 Uvicorn 启动路径
- 统一容器命名规范 (`aiasys-*`)

#### Python 导入路径修复
- 批量修复 56+ 个 Python 文件的导入语句
- `from Agentbackend.app.xxx` → `from app.xxx`
- Celery 任务注册路径更新
- 添加 `sys.path` 修复以支持新的模块结构

###  文档全面更新

#### 根目录文档
- 更新 `README.md` 项目结构说明
- 更新 `CONTRIBUTING.md` 文档导航
- 更新 `LEFTHOOK_SETUP.md` 路径配置
- 更新 `MIGRATION.md` 迁移指南

#### 后端文档 (20+ 个)
- 更新快速开始指南
- 更新架构设计文档
- 更新部署和配置指南
- 更新开发工作流程文档

#### 前端文档 (8+ 个)
- 更新快速开始指南
- 更新文件组织指南
- 更新 Docker 部署指南

#### 部署文档
- 更新 `DEPLOYMENT_GUIDE.md`
- 更新 `DOCKER_STARTUP.md`

###  配置文件更新

#### Git 配置
- 更新 `.gitignore` 路径
- 添加 `archive/` 到忽略列表

#### Lefthook 配置
- 更新 `lefthook.yml` 代码检查路径
- `Agentbackend/**/*.py` → `apps/backend/**/*.py`
- `frontend/**/*` → `apps/web/**/*`
- `supabase/**/*` → `infra/supabase/**/*`

#### 部署脚本
- 更新 `deploy_image.py` 路径引用
- 更新 `start_prod.py` 路径

###  Commit 记录
1.  ` chore: 更新 .gitignore 适配新目录结构`
2.  ` refactor: 删除旧目录结构`
3.  ` feat: 添加新的 apps/ 和 infra/ 目录结构`
4.  ` docs: 更新所有文档路径引用`
5.  ` chore: 添加迁移指南和最终清理`

###  验证结果
- [x] Docker Compose 可正常启动所有服务
- [x] 后端 API 正常运行 (http://localhost:8001)
- [x] 前端正常运行 (http://localhost:7999)
- [x] Celery Worker 正常运行
- [x] Redis 服务正常运行

###  迁移检查清单
- [x] `apps/backend/` 包含完整后端代码
- [x] `apps/web/` 包含完整前端代码
- [x] `infra/docker/docker-compose.yml` 可正常启动
- [x] 所有文档已更新为新的路径引用
- [x] `.gitignore` 已更新

###  注意事项
1. 旧目录 `Agentbackend/` 和 `frontend/` 需要手动删除（如仍被进程占用）
2. 开发时请使用新的目录路径进入项目
3. Docker 命令需要在 `infra/docker/` 目录下执行

###  后续提交记录
6.  ` chore: 更新 .gitignore 配置` - 添加新忽略规则
7.  ` docs: 添加 VS Code 任务配置` - 开发环境配置
8.  ` data: 添加示例数据集` - dabstep 测试数据

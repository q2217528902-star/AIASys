# v0.4.18 (2026-06-20)

## 跨平台兼容性

- `as_system_path()` 接入：`config.py`、`file_utils.py` 等核心文件 IO 路径统一走长路径兼容，Windows 上自动添加 `\\?\` 前缀
- Shell executor 跨平台改进：PTY manager 进程树终止、Runtime execution 编码兼容
- 桌面端 `main.cjs` 改进：WSLg 兼容性参数、Electron 窗口管理、原生菜单优化

## Agent 运行时

- Session mixins 优化：context/control/events/execution/history/session 代码整理与边界加固
- Runtime backend 改进：session_stream 完整性、session_compaction 逻辑优化、memory_tool 编码兼容
- ACP client session 连接稳定性改进
- Monitor/task tools 输出格式优化

## Memory 子系统

- Pipeline 流程优化：job 租约、并发控制、consolidation 入口加固
- Session DB 事务管理改进：`BEGIN IMMEDIATE` + WAL 模式
- Resolver 缓存失效逻辑完善
- Layout 与 state_runtime 边界修正

## Claw 通信

- 删除内部适配器 `_feishu.py`、`_weixin.py`（内部 API 已废弃）
- 微信适配器合并优化，workspace mixins 改进

## AutoTask

- Executor 运行逻辑加固
- Engine 状态管理改进

## CI / 构建

- `ci-desktop.yml` 简化
- `ci.yml` 优化：减少冗余步骤，提升构建速度
- `prepare-runtime.cjs` 跨平台改进
- 二进制下载脚本（fnm/uv/sqlite-vec）兼容性修复

## 前端

- 多个组件稳定性改进：CanvasEditor、AskUserDialog、WorkspaceSidebar、FileMentionPicker 等
- 样式微调与错误边界加固
- `useSubagentStream`、`useTerminal` hooks 优化

## 部署与脚本

- `infra/deploy` 脚本改进
- `release.sh`、`cli.sh` 开发工具优化
- `batch_agent_tests.py` 测试脚本修复
- Lefthook 配置更新

## 其他

- `AGENTS.md` 项目规范更新
- `lefthook.yml` 检查规则调整
- `uv.lock` 依赖锁更新